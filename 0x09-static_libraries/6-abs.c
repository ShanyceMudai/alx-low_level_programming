#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * *Return: always (0)
 *
 * @i: display our values
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

